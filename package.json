{
    "name": "tmdb-proxy",
    "version": "1.0.0",
    "scripts": {
        "cf:login": "wrangler login",
        "cf:dev": "bun run overrides:build && wrangler dev",
        "cf:deploy": "bun run overrides:build && wrangler deploy",
        "overrides:build": "bun run scripts/build-overrides.mjs"
    },
    "devDependencies": {
        "wrangler": "^4.62.0"
    }
}
